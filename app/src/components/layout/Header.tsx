export function Header() {
  return (
    <header className="h-16 flex items-center px-8 border-b border-border bg-white shadow-sm">
      <div className="text-lg font-heading text-primary">Agent Dashboard</div>
      {/* Add user profile, notifications, etc. here */}
    </header>
  );
}
